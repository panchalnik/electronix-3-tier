pipeline {
    agent {
        label 'electronix'
    }

    stages {
        stage('Tools Configure') {
            steps {
                sh '''
                sudo apt-get update -y
                '''
            }
        }
    }
}